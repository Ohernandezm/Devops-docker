pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
/*
    stage('Install Dependencies') {
      steps {
        sh 'npm install -g newman'
        sh 'npm install -g newman-reporter-html'
        sh 'npm install'
      }
    }
    */ 
    stage('Clone repository 1') {
      steps { 
        git branch: 'master',
        credentialsId: 'github_jenkins_access',
        url: 'https://github.com/Ohernandezm/Obrador-copy.git' 
        sh 'cp -r Obrador-copy/* $WORKSPACE/'
      }
    }
    stage('Clone repository 1') {
      steps { 
        git branch: 'master',
        credentialsId: 'github_jenkins_access',
        url: 'https://github.com/Ohernandezm/Devops-docker.git' 
        sh 'cp -r Devops-docker/* $WORKSPACE/'
      }
    }

   /* stage('Build Docker') {
      steps {
        sh 'docker compose up -d'
        sh 'npm run typeorm migration:run'
        sh 'docker ps'
      }
   }

    stage('Test Postman') {
      steps {
        sh 'newman run  ./obrador/Monitor_Obrador.postman_collection.json -e  ./obrador/Obrador_Localhost.postman_environment.json'
      }
    }*/
  }
}
